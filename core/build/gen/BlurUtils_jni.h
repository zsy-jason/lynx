// This file is autogenerated for
//     com/lynx/tasm/utils/BlurUtils

#ifndef com_lynx_tasm_utils_BlurUtils_JNI
#define com_lynx_tasm_utils_BlurUtils_JNI

#include <jni.h>

#include "core/base/android/android_jni.h"
#include "base/include/compiler_specific.h"

// Step 1: forward declarations.
namespace {
const char kBlurUtilsClassPath[] = "com/lynx/tasm/utils/BlurUtils";
// Leaking this jclass as we cannot use LazyInstance from some threads.
jclass g_BlurUtils_clazz = NULL;
#define BlurUtils_clazz(env) g_BlurUtils_clazz

}  // namespace

static
void IterativeBoxBlur(JNIEnv* env, jclass jcaller,
    jobject bitmap,
    jint iterations,
    jint blurRadius);

// Step 2: method stubs.

// Step 3: RegisterNatives.

static const JNINativeMethod kMethodsBlurUtils[] = {
    { "nativeIterativeBoxBlur",
"("
"Landroid/graphics/Bitmap;"
"I"
"I"
")"
"V", reinterpret_cast<void*>(IterativeBoxBlur) },
};

static bool RegisterNativesImpl(JNIEnv* env) {

  g_BlurUtils_clazz = reinterpret_cast<jclass>(env->NewGlobalRef(
      lynx::base::android::GetClass(env, kBlurUtilsClassPath).Get()));

  const int kMethodsBlurUtilsSize =
      sizeof(kMethodsBlurUtils)/sizeof(kMethodsBlurUtils[0]);

  if (env->RegisterNatives(BlurUtils_clazz(env),
                           kMethodsBlurUtils,
                           kMethodsBlurUtilsSize) < 0) {
    //jni_generator::HandleRegistrationError(
    //    env, BlurUtils_clazz(env), __FILE__);
    return false;
  }

  return true;
}

#endif  // com_lynx_tasm_utils_BlurUtils_JNI
